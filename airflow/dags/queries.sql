CREATE TABLE IF NOT EXISTS datasets (
    title VARCHAR(255),
    description TEXT,
    theme VARCHAR(255),
    keyword VARCHAR(255),
    modified DATE,
    publisher VARCHAR(255),
    records_count INT
);
